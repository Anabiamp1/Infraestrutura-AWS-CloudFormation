AWSTemplateFormatVersion: '2010-09-09'
Description: Stack que cria um bucket S3 e uma instância EC2

Parameters:
  InstanceType:
    Description: Tipo da instância EC2
    Type: String
    Default: t2.micro
    AllowedValues:
      - t2.micro
      - t2.small
      - t2.medium

Resources:
  MeuBucketS3:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: meu-bucket-dio-anabe-2025-002

  SecurityGroupEC2:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Permitir acesso SSH e HTTP
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0

  MinhaInstanciaEC2:
    Type: AWS::EC2::Instance
    Properties:
      InstanceType: !Ref InstanceType
      ImageId: ami-0c02fb55956c7d316  # Amazon Linux 2 na região us-east-1
      SecurityGroups:
        - !Ref SecurityGroupEC2
      Tags:
        - Key: Name
          Value: MinhaInstanciaDIO

Outputs:
  BucketName:
    Description: Nome do bucket S3 criado
    Value: !Ref MeuBucketS3

  EC2InstanceId:
    Description: ID da instância EC2 criada
    Value: !Ref MinhaInstanciaEC2
